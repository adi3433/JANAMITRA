apiVersion: apps/v1
kind: Deployment
metadata:
  name: janamitra
  labels:
    app: janamitra
spec:
  replicas: 2
  selector:
    matchLabels:
      app: janamitra
  template:
    metadata:
      labels:
        app: janamitra
    spec:
      containers:
        - name: janamitra
          image: ghcr.io/sveep-kottayam/janamitra:latest
          ports:
            - containerPort: 3000
          envFrom:
            - secretRef:
                name: janamitra-secrets
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: janamitra
spec:
  selector:
    app: janamitra
  ports:
    - port: 80
      targetPort: 3000
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: janamitra
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - janamitra.sveep-kottayam.gov.in
      secretName: janamitra-tls
  rules:
    - host: janamitra.sveep-kottayam.gov.in
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: janamitra
                port:
                  number: 80
